# Формат файла стиля для SRA (Software-based Real-time Arranger)

## 1. Общие сведения

Файл стиля SRA — это **стандартный MIDI-файл формата SMF Format 0** (не Format 1).
Файлы называются `style0.mid` … `style127.mid` и размещаются в той же директории,
что и исполняемый файл SRA.

Все параметры и музыкальные данные находятся в **одном треке** (`MTrk`).
Формат 0 означает, что все MIDI-каналы пишутся в одну дорожку, а не разбиваются по трекам.

---

## 2. Структура файла
```text
MThd (заголовок, 14 байт)
MTrk (единственный трек, переменная длина)
├── Такт 1 (служебный): 5 параметров + служебные события
├── Такт 2 и далее: музыкальные данные
│ ├── Блок для аккорда C (20 тактов при IL=4, ML=4, EL=6)
│ ├── Блок для аккорда Cm (20 тактов)
│ └── Блок для аккорда C7 (20 тактов)
└── Meta:EndOfTrack
```

---

## 3. Первый (служебный) такт

Первый такт содержит **пять параметров**, которые SRA считывает при загрузке.
Эти параметры кодируются нестандартным способом — через **velocity служебных
NoteOn-событий на канале 1 с нотой 0**.

### 3.1. Служебные NoteOn-события

В первом такте на канале 1 создаются **пять NoteOn-событий** с нотой `0` (C-1).
Velocity этих событий и есть значения параметров:

| № события | Velocity | Параметр SRA | Диапазон | Описание |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 10–125 | **Tempo** | 10–125 | Начальный темп. **Вводится половина фактического BPM** (для 120 BPM → 60) |
| 2 | 2, 3, 4, 6 | **Beat** | 2, 3, 4, 6 | Количество долей в такте |
| 3 | 1–8 | **IL** | 1–8 | Количество тактов во вступлении (Intro Length) |
| 4 | 1–8 | **ML** | 1–8 | Количество тактов в основной секции (Main Length) |
| 5 | 1–8 | **EL** | 1–8 | Количество тактов в концовке (Ending Length) |

**Важно:** нота `0` — это не музыкальная нота, а «маркер» служебного события.
В реальном аккомпанементе нота 0 не используется (это C-1, самая низкая нота MIDI).

### 3.2. Обязательные Meta-события первого такта

Помимо пяти NoteOn, в первом такте должны присутствовать стандартные Meta-события:

- **`Meta:TimeSig`** — размер такта. Числитель = `Beat`.
  Пример: `ff 58 04 04 02 18 08` = 4/4.
- **`Meta:SetTempo`** — фактический темп в микросекундах на четверть.
  SRA использует его для воспроизведения.
  Пример: `ff 51 03 06 d5 11` = 447761 µs ≈ 134 BPM.
- **`Meta:Marker`** — необязательное событие, можно оставить пустым или с любым текстом.

***Примечание**: не факт, что все эти Meta-события обязательные. Не факт даже, что они реально используются самой программой SRA.*

### 3.3. Пример структуры первого такта (по дампу style0.mid)

```text
tick 0: Meta:TimeSig (4/4)
tick 0: Meta:KeySig (C Maj)
tick 0: Meta:SetTempo (134 BPM)
tick 0: Meta:Marker "untitled"
tick 0: NoteOn ch1 note0 velo67 → Tempo = 67 (половина от 134)
tick 0: NoteOn ch1 note0 velo0 → (разделитель, игнорируется)
tick 0: NoteOn ch1 note0 velo4 → Beat = 4
tick 0: NoteOn ch1 note0 velo0
tick 0: NoteOn ch1 note0 velo4 → IL = 4
tick 0: NoteOn ch1 note0 velo0
tick 0: NoteOn ch1 note0 velo4 → ML = 4
tick 0: NoteOn ch1 note0 velo0
tick 0: NoteOn ch1 note0 velo6 → EL = 6
tick 0: NoteOn ch1 note0 velo0
```

**Нулевые velocity** между ненулевыми — это NoteOff для предыдущей ноты 0.
Они не несут смысловой нагрузки, но их наличие не мешает.

---

## 4. Тело файла (со второго такта)

### 4.1. Порядок секций

Начиная со второго такта (tick = 480 при 120 PPQ и 4/4), музыкальные данные
располагаются в следующем порядке:

```text
Блок 1 (Chord C):
  Intro — IL тактов
  Original — ML тактов
  Fill (Orig→Var) — 1 такт
  Variation — ML тактов
  Fill (Var→Orig) — 1 такт
  Ending — EL тактов

Блок 2 (Chord Cm): то же самое
Блок 3 (Chord C7): то же самое
```

**Маркеры секций не используются.** SRA определяет границы секций
**арифметически**, отсчитывая такты от начала второго такта, используя
значения IL, ML, EL из первого такта. В полном дампе `style0.mid` нет
ни одного `Meta:Marker` после первого такта — только NoteOn, ProgChg,
PitchBend и EndOfTrack.

### 4.2. Инициализация в начале каждого такта

**Каждый такт** должен начинаться с команд инициализации — Program Change
и/или Bank Select для всех используемых каналов. Без этого SRA вернёт `Error(5)`.

В `style0.mid` в начале **каждого** такта стоят:

```text
ProgChg ch2 pg32 (Acc.Bass)
ProgChg ch5 pg0 (Acc1)
ProgChg ch7 pg49 (Acc3)
ProgChg ch10 pg0 (Drum)
```

Значения **не меняются** от такта к такту — они дублируются.
Это требование SRA, а не художественный приём.

### 4.3. Распределение MIDI-каналов

| Канал | Название | Роль | Патч в style0.mid |
| :--- | :--- | :--- | :--- |
| Ch2 | Acc.Bass | Автобас | pg32 |
| Ch5 | Acc1 | Аккомпанемент 1 | pg0 |
| Ch6 | Acc2 | Аккомпанемент 2 | (не используется) |
| Ch7 | Acc3 | Аккомпанемент 3 (тянущиеся звуки) | pg49 |
| Ch8 | Acc4 | Аккомпанемент 4 (тянущиеся звуки) | (не используется) |
| Ch10 | Drum | Ударные | pg0 |

Каналы Ch6 и Ch8 зарезервированы, но в конкретном стиле могут быть пустыми.

### 4.4. Running status

В MIDI-потоке может использоваться **running status** — механизм сжатия,
при котором повторяющийся статус-байт опускается. В дампе это помечено `(RunStat)`.
MIDI-редактор **не обязан** сохранять running status — файл без него тоже корректен.
SRA читает оба варианта.

---

## 5. Пошаговая инструкция по созданию файла стиля

### Шаг 1: Создание проекта

1. Откройте любой MIDI-редактор (Cakewalk, FL Studio, Reaper, Anvil Studio и т.п.).
2. Создайте новый проект.
3. **Установите формат SMF Format 0** (в настройках экспорта/сохранения).
4. Установите `TimeBase` (PPQ) = 120 (стандарт для SRA).

### Шаг 2: Первый такт — параметры

1. Установите **`Meta:TimeSig`** = нужный размер (числитель = Beat: 2, 3, 4 или 6).
2. Установите **`Meta:SetTempo`** = фактический BPM (например, 134).
3. Создайте **пять NoteOn-событий на канале 1, нота 0**:
   - 1-е: velocity = половина BPM (67 для 134)
   - 2-е: velocity = Beat (4)
   - 3-е: velocity = IL (4)
   - 4-е: velocity = ML (4)
   - 5-е: velocity = EL (6)
4. Все события — на `tick = 0`.

### Шаг 3: Тело файла

1. Начиная с `tick = 480` (второй такт), запишите музыку в порядке:
   - Intro (IL тактов)
   - Original (ML тактов)
   - Fill (1 такт)
   - Variation (ML тактов)
   - Fill (1 такт)
   - Ending (EL тактов)
2. Повторите этот блок **три раза** — для C, Cm, C7.
3. **В начале каждого такта** поставьте Program Change для каналов Ch2, Ch5, Ch7, Ch10.
4. Используйте ноты только на каналах Ch2, Ch5, Ch7, Ch10 (и Ch6, Ch8 при необходимости).

### Шаг 4: Сохранение

1. Сохраните файл как `style0.mid` (или `style1.mid` … `style127.mid`).
2. Поместите файл в директорию с исполняемым файлом SRA.
3. Проверьте загрузку через Function Mode (`Shift+Ab7` + клавиша стиля).

---

## 6. Типичные ошибки

| Ошибка | Причина | Решение |
| :--- | :--- | :--- |
| `Error(2)` | Сохранён Format 1 | Пересохранить в Format 0 |
| `Error(3)` | Параметры вне диапазона | Проверить velocity служебных NoteOn |
| `Error(4)` | Недопустимый MIDI-канал | Использовать только Ch2, 5, 6, 7, 8, 10 |
| `Error(5)` | Нет инициализации в такте | Добавить ProgChg в начало каждого такта |
| `Error(6)` | Недостаточно тактов | Проверить IL, ML, EL и количество тактов |

---

## 7. Ключевые нестандартные особенности

1. **Нота 0** на канале 1 — служебная, не музыкальная.
2. **Velocity = параметр** — пять NoteOn кодируют Tempo, Beat, IL, ML, EL.
3. **Tempo вводится вполовину** — в служебном NoteOn velocity,
   а `Meta:SetTempo` содержит фактический BPM.
4. **Нет маркеров секций** — границы определяются арифметически по IL/ML/EL.
5. **ProgChg дублируются в каждом такте** — требование SRA,
   а не изменение инструментов.
6. **Running status** — опционален, редактор может использовать или не использовать.
